#!/bin/bash
#SBATCH --job-name={{ job_name }}
#SBATCH --nodes={{ nodes }}
#SBATCH --ntasks-per-node=1
#SBATCH --cpus-per-task={{ cpus_per_task }}
#SBATCH --gpus-per-node={{ gpus_per_node }}
#SBATCH --partition={{ partition }}
#SBATCH --account={{ account }}
#SBATCH --time={{ time_hours }}:00:00
{% if email %}#SBATCH --mail-user={{ email }}
#SBATCH --mail-type=END
#SBATCH --mail-type=FAIL
{% endif %}

# Generated by CasMD bundle packager.
# Edit the SLURM directives above to match YOUR cluster if these defaults
# (HKUST Hsing Lab HPC4 / gpu-l20) don't apply.

set -euo pipefail

{% if spack_env %}spack env activate {{ spack_env }}
{% endif %}

bash run_md.sh
